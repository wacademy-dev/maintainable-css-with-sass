<h3>Lesson 15, Exercise 2</h3>
<p>Let&rsquo;s clean house a bit! Our Sass file is getting a bit long, and all that scrolling is getting obnoxious. We can get started by setting up the directories to organize our code with the 7-1 file system.</p>
<div><strong>Tasks</strong></div>
<ul>
<li>Create a file in the&nbsp;<code>utils/</code>&nbsp;directory named&nbsp;<code>_variables.scss</code>&nbsp;<em>The underscore prefixing the name is important!</em></li>
<li>Move the variables living in&nbsp;<code>main.scss</code>&nbsp;into our new variables partial.</li>
<li>Import the variables partial into&nbsp;<code>main.scss</code>&nbsp;via the&nbsp;<code>@import</code>&nbsp;keyword and the path to the variables file.</li>
<li>Review the rendered page, ensuring that everything still renders properly</li>
<li>Review the compiled CSS. The variables should exist where we used the<code>@import</code>&nbsp;keyword.</li>
<li>For extra practice create, fill, and import partials for&nbsp;<code>layout/_form.scss</code>,&nbsp;<code>layout/_nav.scss</code>,&nbsp;<code>layout/_header.scss</code>,&nbsp;<code>utils/mixins.scss</code>,&nbsp;<code>utils/functions.scss</code>, etc.</li>
</ul>