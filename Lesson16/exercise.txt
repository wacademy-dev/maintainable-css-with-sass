<h3>Lesson 16, Exercise</h3>
<p>Let&rsquo;s give our CSS some structure and put our new-found nesting knowledge to use. Right now, we have a&nbsp;<code>.parent</code>&nbsp;selector, and a&nbsp;<code>.child</code>&nbsp;selector. Let&rsquo;s organize them to reflect their relationship using Sass&rsquo; nesting by placing the&nbsp;<code>.child</code>&nbsp;rule set within the&nbsp;<code>.parent</code>&nbsp;rule set.</p>
<p><strong>Tasks</strong></p>
<ul>
<li>In the terminal type&nbsp;<code>npm init</code>, and answer the cues as you see fit</li>
<li>In the terminal type&nbsp;<code>npm install sass -g</code>&nbsp;to install Sass globally on the system</li>
<li>Add a key to the &ldquo;scripts&rdquo; object called &ldquo;sass&rdquo;</li>
<li>For the sass keys value, write the script to compile the CSS to&nbsp;<code>public/css/styles.css</code></li>
<li>Review the console to ensure that Sass is re-compiling the CSS upon saving Sass files</li>
</ul>